<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link rel="stylesheet" href="/styles.css">
</head>

<div class="wrapper">
    <header>
        <h1>Pet Spot</h1>
        <nav>
            {{#if loggedIn}}
            <a href="/" class="navbar-item">Homepage</a>
            <a href="/dashboard" class="navbar-item">Dashboard</a> {{!-- Has no route or handlebars. --}}
            <a href="/dashboard/create" class="navbar-item">Create Post</a> {{!-- Has no route or handlebars. --}}
            <button id="logout" class="button">Logout</button>
            {{else}}
            <a href="/" class="navbar-item">Homepage</a>
            <a href="/login" class="button">Login</a>
            {{/if}}
        </nav>
    </header>

    <main>

        {{{body}}}

    </main>

    {{#if loggedIn}}
    <script src="/js/logout.js"></script>
    {{/if}}
</div>

</html>