
{{#each posts as |post|}}

{{> post-info post }}
</div>

{{else}}
<h6 class="post">There are no posts right now.</h6>
{{/each}}


{{!-- This might need its own 'handlebars' file: --}}

{{!-- <div class="text-center">
    <h3>Comments</h3>
    {{#each comments as |comment|}}
    <div>
      <p>{{comment.comment_body}} by {{comment.user.name}}</p>
      {{/each}}
  </div>
  {{ > comment-form }}
  {{! still figuring out how to connect the comment form}}
  
  <div>
  {{#each Posts as |Post|}}
    <h2>{{post.id}}</h2>
  {{/each}}  

</div> --}}

